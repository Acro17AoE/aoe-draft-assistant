# Frontend (Vite + React)

See the repository root [README.md](../README.md) for full setup.

```bash
npm install
npm run dev
```

The Vite dev server proxies `/api` to the FastAPI backend (`localhost:8000` by default).
